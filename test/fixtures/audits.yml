# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one: 
  audited_changes: { name: ['old_value', 'new_value'] }
  user: one
  auditable: one (Question)

two:
  audited_changes: { description: ['old_value', 'new_value'] }
  user: two
  auditable: one (Election)
